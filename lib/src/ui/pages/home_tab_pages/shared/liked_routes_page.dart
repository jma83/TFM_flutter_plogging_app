import 'package:flutter/material.dart';

class LikedRoutesPage extends StatelessWidget {
  const LikedRoutesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
