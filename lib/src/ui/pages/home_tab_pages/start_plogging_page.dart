import 'package:flutter/material.dart';

class StartPloggingPage extends StatelessWidget {
  const StartPloggingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
